import React from 'react';
import Post from '../Post'

function PostsContainer(){
    return(
      <div>
      {//iterate over posts and render post Component for each

      }
        <Post />
      </div>
    )
}

export default PostsContainer;
